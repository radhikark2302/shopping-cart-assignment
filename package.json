{
  "name": "shopping-cart-assignment",
  "version": "1.0.0",
  "description": "Shopping cart assignment for interviews",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start:api": "npm run serve-products",
    "test": "jest",
    "serve-products": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "json-server": "^0.17.4",
    "ts-jest": "^29.2.5"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "node-fetch": "^2.7.0"
  }
}
